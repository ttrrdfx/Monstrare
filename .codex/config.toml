# Optional project-level Codex defaults.
# Keep this file conservative. Users should adapt sandbox,
# model, and MCP settings to their own environment.

# Optional model overrides:
# model = "gpt-5.4-codex"
# model_reasoning_effort = "medium"

# Keep writes scoped to the project.
sandbox_mode = "workspace-write"

