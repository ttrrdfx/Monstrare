# 驗證報告

## 摘要

- 任務：TASK-007 看板啟動時自動避開已占用的 port
- 結果：通過
- 驗證者：Codex

## 指令

| 指令 | 結果 | 備註 |
|---|---|---|
| `node --test tools/kanban/server-events.test.mjs` | 通過 | 3/3；包含占用起始 port 後自動遞增並確認 API 可用 |
| `npm run check` | 通過 | 18/18 測試、server syntax 與治理檔案檢查全部通過 |
| `git diff --check` | 通過 | 無空白字元錯誤 |

## UI 證據

| Viewport | 螢幕截圖 | 備註 |
|---|---|---|
| 不適用 | 不適用 | 本次只修改本機 server 啟動行為與文字說明，沒有 UI 視覺變更 |

## 審查發現

| 發現 | 嚴重程度 | 狀態 |
|---|---|---|
| `EADDRINUSE` 原本會直接中止程序 | 低 | 已修正為依序嘗試下一個有效 port |
| 自訂 `KANBAN_PORT=0` 的動態 port 測試行為 | 低 | 保持不變 |

## 殘留風險

- 若從起始 port 一路到 65535 都無法監聽，程序仍會以啟動錯誤結束。
- 只監聽 `127.0.0.1` 的既有安全邊界維持不變。
