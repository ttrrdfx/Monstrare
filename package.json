{
  "name": "monstrare",
  "private": true,
  "description": "A gate-based governance workflow for AI coding agents. Nothing ships without evidence.",
  "license": "MIT",
  "scripts": {
    "kanban": "node tools/kanban/server.mjs",
    "test": "node --test tools/kanban/*.test.mjs test/monstrare/*.test.mjs",
    "check": "npm test && bash scripts/check-syntax.sh && bash scripts/check-governance.sh"
  }
}
