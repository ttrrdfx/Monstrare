{
  "name": "monstrare",
  "private": true,
  "description": "A gate-based governance workflow for AI coding agents. Nothing ships without evidence.",
  "license": "MIT",
  "scripts": {
    "kanban": "node tools/kanban/server.mjs",
    "test": "node --test tools/kanban/*.test.mjs",
    "check": "npm test && node --check tools/kanban/server.mjs && bash scripts/check-governance.sh"
  }
}
